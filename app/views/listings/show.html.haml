/ Top Main Nav
= render 'nav_1'
/ Top Sub Nav 3
= render 'nav_3'

/ Listing title, and description
.each-listing-heading
  %h1
    = @listing.title
  
  %h4
    = link_to user_path(@listing.user), :class => "list-user-link" do
      = @listing.user.username
  - if @listing.user_auth(current_user)
    .each-listing-options
      #option-1
        = link_to 'Add Product Image', new_listing_image_path(@listing.id) 
      #option-2
        = link_to 'Edit Listing', edit_listing_path(@listing.id)
  %h3
    %span{:style => "color: #FEFEFE;"} Price:
    %span{:style => "color: rgba(255, 153, 0, 0.5)"}
      = number_to_currency(@listing.price, :unit =>"$")
  %p
    = @listing.description
/ Carousel of listing images
.listing-image.parent-container
  - @listing.images.each do |image|
    %div
      .image-container
        = image_tag image.picture_url, :class => "each-image"